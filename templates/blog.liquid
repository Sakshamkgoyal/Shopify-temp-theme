<h1>{{blog.title}}</h1>  

{% for article in blog.articles %}
    <div>
        <a href="{{ articles.url }}" >
            <h2>{{article.title }}</h2>
            <p>{{article.author}}</p>
            <div>{{article.excerpt}}</div>
        </a>
    </div>
{%endfor%}